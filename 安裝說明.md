# 安裝說明

學生作業繳交系統 — 安裝在老師電腦上，學生用校內網路連進來繳交作業。

> **給誰看的**：負責安裝的老師或資訊組同仁。不需要會寫程式，照著步驟做即可。
> 需要用到命令列的地方都會寫出完整指令，可直接複製貼上。

---

## 先確認三件事

在開始之前，先確認這三項，可以省下很多時間：

| 項目 | 怎麼確認 | 不符合的話 |
|---|---|---|
| 作業系統 | Windows 10 或 11 | 其他系統請看文件最後的〈附錄 B〉 |
| 學生電腦連得到老師電腦 | 見下方〈網路連通性測試〉 | **這項不通就不用往下做了**，要先找網管 |
| 老師電腦有系統管理員權限 | 能不能用「以系統管理員身分執行」 | 設定防火牆時會需要，要先找資訊組 |

### 網路連通性測試

**這是整個安裝裡最該先做的一步。** 學校常把學生 WiFi 和教師網路做網段隔離，
隔離的話學生就是連不到老師電腦，這是網路設定問題，程式端無解。

1. 在**老師電腦**按 `Win + R`，輸入 `cmd` 按 Enter，執行：

   ```
   ipconfig
   ```

   找到「IPv4 位址」，記下來（例如 `192.168.1.50`）。

2. 到**任何一台學生電腦**，同樣開 cmd，執行（把 IP 換成剛才記下的）：

   ```
   ping 192.168.1.50
   ```

3. 看結果：

   - 出現「回覆自 192.168.1.50: 位元組=32...」→ **通了**，可以往下做
   - 出現「要求等候逾時」或「無法連線」→ **不通**，先找網管處理網段隔離

> `ping` 不通但之後想再試一次的話：有些環境會擋 ping 卻允許網頁連線，
> 所以即使 ping 不通，也還是可以把系統裝好之後直接用瀏覽器試連看看。
> 但如果 ping 通了，網頁一定也會通（防火牆設定好的前提下）。

---

## 安裝步驟

### 步驟 1：安裝 Node.js

到 <https://nodejs.org> 下載 **LTS 版本**（畫面上會標示 LTS，版本號要 20 以上），
一路按「下一步」安裝完成即可，不需要改任何選項。

裝完後開 cmd 執行下面這行確認：

```
node -v
```

有印出 `v20.x.x` 或更高就成功了。

### 步驟 2：把專案資料夾複製到老師電腦

整個 `work_student` 資料夾複製過去，建議放在不會被誤刪的地方，例如：

```
C:\作業繳交系統\
```

> **不要放在「下載」或桌面**，也不要放在會被同步軟體（OneDrive、Google Drive）
> 監控的資料夾 —— 資料庫檔案被同步軟體鎖住會出問題。

### 步驟 3：★ 先改班級名單（順序很重要）

**這一步一定要在第一次啟動之前做完。**

系統建立資料庫時會一併建立帳號，而且**只會建立一次**。一旦啟動過，
之後再改名單就沒有作用了，只能整個清空重來（連學生已上傳的檔案也會一起刪掉）。

用記事本打開：

```
apps\api\src\db\seed.ts
```

找到這一段：

```ts
      { email: 's1130101@example.edu.tw', name: '陳小明', studentNo: '1130101', role: 'student' as const, passwordHash },
      { email: 's1130102@example.edu.tw', name: '林小華', studentNo: '1130102', role: 'student' as const, passwordHash },
      { email: 's1130103@example.edu.tw', name: '黃小美', studentNo: '1130103', role: 'student' as const, passwordHash },
```

照著格式改成真實名單，一位學生一行，逗號不要漏掉：

```ts
      { email: 's1130101@nysvs.edu.tw', name: '王大明', studentNo: '1130101', role: 'student' as const, passwordHash },
      { email: 's1130102@nysvs.edu.tw', name: '李小美', studentNo: '1130102', role: 'student' as const, passwordHash },
```

同一段落往上一點還有老師與課程資料，也一併改掉：

```ts
    .values({ email: 'teacher@example.edu.tw', name: '王老師', role: 'teacher', passwordHash })
```
```ts
    .values({ code: 'CS101', title: '資訊科技概論', term: '114-1', ownerId: teacher.id })
```

> **所有人的初始密碼都是 `Passw0rd!`**，寫在同一個檔案上方的 `DEMO_PASSWORD`。
> 目前系統還沒有「修改密碼」功能，詳見〈目前的限制〉。

### 步驟 4：建立設定檔

在專案資料夾裡，把 `.env.production.example` 複製一份改名為 `.env`。
用 cmd 的話：

```
cd /d C:\作業繳交系統
copy .env.production.example .env
```

接著產生一組安全金鑰：

```
node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
```

會印出一長串亂碼，**全部複製**，用記事本打開 `.env`，把這一行：

```
JWT_SECRET=請換掉這一行
```

改成（等號後面貼上剛才複製的亂碼）：

```
JWT_SECRET=剛才複製的那串亂碼
```

存檔關閉。

> 這串金鑰用來簽發登入憑證。沒換掉的話系統會拒絕啟動。
> 之後若更換這串金鑰，所有人都需要重新登入（資料不會遺失）。

### 步驟 5：設定防火牆

在專案資料夾找到 **`允許學生連線.bat`**，
**用滑鼠右鍵 → 以系統管理員身分執行**。

只需要做這一次。它會開放校內網路連進來，並清掉可能存在的「封鎖 Node.js」舊規則。

> 這一步跳過的話，學生會連不進來，而且**伺服器端完全看不到任何訊息** ——
> 因為封包在進到程式之前就被防火牆丟掉了。這是最常卡住又最難查的環節。

### 步驟 6：啟動

雙擊 **`啟動伺服器.bat`**。

第一次執行會自動安裝套件、建置程式、建立資料庫，需要幾分鐘，畫面會一直捲動，
這是正常的。看到類似下面的訊息就代表成功了：

```
API 已啟動：http://localhost:3000/api/health
資料庫 driver=pglite，儲存 driver=local
前端由 API 托管：C:\作業繳交系統\apps\web\dist
登入 cookie：非 Secure（可走純 HTTP；僅適用於校內區網等封閉環境）
本機開啟：http://localhost:3000
同一個區網的學生請連：http://192.168.1.50:3000
```

**最後一行就是要給學生的網址。**

### 步驟 7：實際測試

1. 在老師電腦的瀏覽器打開 `http://localhost:3000`，用老師帳號登入，出一份測試作業並發布
2. **到一台真正的學生電腦**，瀏覽器輸入 `http://192.168.1.50:3000`（換成你的 IP）
3. 用學生帳號登入，上傳一個檔案並送出
4. 回到老師電腦，確認看得到那份繳交紀錄，而且下載得下來

> 第 2 步一定要用**另一台電腦**測，不能只在老師電腦上測 ——
> 在同一台電腦上測不會經過防火牆，測過了不代表學生連得進來。

---

## 每天怎麼用

**開始上課前**：雙擊 `啟動伺服器.bat`，把畫面上的網址寫在黑板。

**結束後**：直接關掉那個黑色視窗，或在視窗中按 `Ctrl + C`。

**注意**：那個視窗關掉系統就停了，學生就交不了作業。收作業期間請保持開著，
並到「設定 → 系統 → 電源」把睡眠設成「永不」。

---

## 給學生的說明

可以直接抄在黑板上：

```
1. 開啟瀏覽器（Chrome / Edge 都可以）
2. 網址列輸入：http://192.168.1.50:3000
3. 帳號：你的學號（例如 1130101）
   密碼：Passw0rd!
4. 點選課程 → 選作業 → 上傳檔案 → 按「送出」
   ★ 只有按了「送出」才算繳交，只上傳檔案不算
```

學生端**不需要安裝任何東西**，手機和平板也可以用。

---

## 預設帳號

| 角色 | 帳號 | 密碼 |
|---|---|---|
| 老師 | `teacher@example.edu.tw` | `Passw0rd!` |
| 學生 | 學號（如 `1130101`）或 email | `Passw0rd!` |
| 管理員 | `admin@example.edu.tw` | `Passw0rd!` |

學生可以用**學號**或 email 登入，兩種都行。

---

## 備份

要備份的只有兩個資料夾：

| 資料夾 | 內容 |
|---|---|
| `apps\api\.data` | 資料庫（帳號、課程、作業、繳交紀錄、繳交時間） |
| `apps\api\.storage` | 學生上傳的檔案本體 |

**先關掉伺服器**（關掉那個黑色視窗），再複製這兩個資料夾到隨身碟或學校網路磁碟。
建議每次收完作業就備份一次。

> 兩個資料夾要**成對**備份與還原。只還原其中一個會造成資料庫有紀錄但檔案不見，
> 或是有檔案但系統看不到。

---

## 目前的限制

這些是安裝前就該知道的，不是安裝錯誤：

### 還不能自己新增學生或改密碼

系統目前**沒有使用者管理介面**。新增學生、修改密碼都必須改 `seed.ts`
並在**資料庫還是空的時候**執行。所以〈步驟 3〉才必須排在啟動之前。

已經啟動過又想改名單的話，只能執行 `npm run db:reset` 全部清空重來 ——
**連學生已上傳的檔案也會一起刪掉**。開學前請先確認名單。

### 所有人初始密碼相同，且無法修改

目前所有帳號的密碼都是 `Passw0rd!`，學生無法自行修改。
也就是說**學生互相知道彼此的密碼**，理論上可以互相登入。

在校內、老師在場的環境下風險有限，但若要正式當成成績依據，
建議先補上修改密碼功能。

### 連線未加密

走的是純 HTTP，帳號密碼在校內網路中是未加密傳送的。
封閉的校內網路可以接受，但**不要**把這台電腦開放到外網。

### 尚未接防毒掃描

學生上傳的檔案會檢查副檔名與檔案內容是否相符（例如把 `.php` 改名成 `.pdf` 會被擋下），
但**沒有掃毒**。老師下載開啟學生檔案前請留意。

### 只能校內連線

學生在家連不進來。若有需要，要另外處理對外連線，而且屆時應一併架設 HTTPS。

---

## 疑難排解

| 症狀 | 原因 | 處理方式 |
|---|---|---|
| 學生連不上，老師電腦自己連得上 | 防火牆沒設定 | 以系統管理員身分執行 `允許學生連線.bat` |
| 學生連不上，`ping` 也不通 | 網段隔離 | 找網管，程式端無法處理 |
| 昨天可以，今天學生連不上 | IP 變了 | 重看啟動畫面的網址；請網管配固定 IP |
| 登入後又跳回登入畫面 | `.env` 的 `COOKIE_SECURE` 設成了 `true` | 改回 `false`（純 HTTP 環境必須是 false） |
| 啟動時說「port 已被佔用」 | 3000 被其他程式用了 | 照畫面指示改 `.env` 的 `PORT`，例如改成 `3100` |
| 啟動時說「JWT_SECRET 還沒換掉」 | 步驟 4 沒做完 | 回去做步驟 4 |
| 學生上傳失敗 | `.env` 裡設了 `API_PUBLIC_URL` | 把那一行整行刪掉或註解掉 |
| 學生說「送出」按鈕沒反應 | 沒選檔案也沒填內容 | 至少要上傳一個檔案或填寫作答內容 |
| 過了截止時間交不了 | 正常行為 | 老師可修改作業的截止或補交時間 |

> 遇到表格裡沒有的狀況，把**黑色視窗上的訊息截圖**下來，那裡通常已經寫明原因。

---

## 附錄 A：`.env` 設定說明

只有這幾項需要在意，其餘保持預設即可：

| 設定 | 預設 | 說明 |
|---|---|---|
| `PORT` | `3000` | 服務的連接埠。改了之後學生的網址也要跟著改 |
| `JWT_SECRET` | 必須自行更換 | 登入憑證的簽章金鑰 |
| `COOKIE_SECURE` | `false` | **純 HTTP 環境必須是 `false`**，否則登不進去 |
| `API_PUBLIC_URL` | 不要設定 | 留空系統才會自動適應學生連進來的 IP |
| `NODE_ENV` | `production` | 讓系統進入正式模式並托管前端 |

---

## 附錄 B：不用 `.bat` 的手動安裝

macOS、Linux，或想自己掌握每個步驟的話：

```bash
npm install                     # 安裝相依套件
npm run build                   # 建置
npm run db:migrate              # 建立資料表
npm run db:seed                 # 建立帳號與範例資料
npm start                       # 啟動
```

`.env` 的設定與〈步驟 4〉相同。防火牆請依該系統的方式開放對應連接埠。

---

## 附錄 C：確認系統是否正常運作

伺服器啟動後，在瀏覽器打開：

```
http://localhost:3000/api/health
```

正常會看到類似這樣的回應：

```json
{"ok":true,"db":"pglite","storage":"local","time":"2026-09-04T00:00:00.000Z"}
```

看到這個就代表後端與資料庫都正常。
