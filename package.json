{
  "name": "work-student",
  "private": true,
  "version": "0.1.0",
  "description": "網頁版學生作業繳交系統 (M1)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c green,cyan \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev -w @ws/api",
    "dev:web": "npm run dev -w @ws/web",
    "build": "npm run build -w @ws/shared && npm run build -w @ws/api && npm run build -w @ws/web",
    "db:migrate": "npm run db:migrate -w @ws/api",
    "db:seed": "npm run db:seed -w @ws/api",
    "db:reset": "npm run db:reset -w @ws/api",
    "typecheck": "npm run typecheck -w @ws/shared && npm run typecheck -w @ws/api && npm run typecheck -w @ws/web",
    "test:e2e": "npm run test:e2e -w @ws/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20"
  }
}
